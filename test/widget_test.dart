// Placeholder test — the real app uses camera/TFLite which require a physical
// device and cannot be widget-tested in this environment.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
